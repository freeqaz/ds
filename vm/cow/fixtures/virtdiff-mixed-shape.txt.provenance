{"ds_fixture":{"provenance":"synthetic","seam":"vm/cow enumerate-writes virt-diff parse (D29; doc 01 sessions/01 spike)","created":"2026-06-13","note":"Hand-authored synthetic GENUINELY-INTERLEAVED `virt-diff` capture for a DESTROYED session overlay: a leading Comparing banner (skipped) followed by plain-shaped rows ('= /path', '+ /path\\t<stat>', '- /path') and CSV-shaped rows ('+,\"/path\"', '=,\"/path\",<stat>', '-,\"/path\"') mixed in ONE capture, as if two appended dumps. Models the silent-degrade case: the whole-capture auto-detect commits to ONE shape from the first (plain) data row, so a forced --mode-plain ERRORS on the CSV rows while a forced --mode-csv silently UNDER-REPORTS (drops the 4 plain rows). ModeAuto RECOVERS it via the per-row classifier (DetectedMode=mixed), parsing all 7 writes (2 added, 3 modified, 2 deleted) with every path WHOLE — embedded spaces survive in both shapes (RFC-4180 quoting for CSV, right-edge stat-peel for plain). Invented agent-workspace paths only; never a real session capture, never a real credential (D50). Drives the enumerate-writes.sh --self-test degrade-then-override flow and TestParseVirtDiff_MixedShapeFixture."}}

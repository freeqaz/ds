{"ds_fixture":{"provenance":"synthetic","seam":"vm/cow enumerate-writes qemu-img backing-chain parse (D29)","created":"2026-06-13","note":"Hand-authored synthetic `qemu-img info --backing-chain overlay.qcow2` output: a per-session qcow2 overlay whose backing file is the raw M0 base (one backing level, base→overlay). Encodes the D29 invariant the create path establishes — the overlay layers on a raw base. Invented session/base paths only; never a real image (D50). Positive control: parses to OverlayPath=overlay.qcow2, BasePath=m0-base.raw."}}

{"ds_fixture":{"provenance":"synthetic","seam":"vm/cow enumerate-writes qemu-img backing-chain parse (D29)","created":"2026-06-13","note":"Hand-authored NEGATIVE fixture: `qemu-img info` output for a qcow2 with NO `backing file:` line — i.e. an image that is NOT a per-session overlay over the raw base. A session overlay MUST layer on the read-only base (D29); a backing-less image means the create-path invariant was violated, so the parser MUST error. Invented session path only; never a real image (D50). Must FAIL ParseQemuImgInfo."}}

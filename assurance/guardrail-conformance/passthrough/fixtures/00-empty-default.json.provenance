{"ds_fixture":{"provenance":"synthetic","seam":"guardrail-conformance/passthrough pass-through-empty-by-default (doc 06 §3c; D17/TLS-4; D74)","created":"2026-06-15","note":"hand-authored pass-through config: the shipped baseline (is_default=true) with an empty pass-through list, every endpoint TLS-terminated at the per-session CA, swap only on the terminated path. Models the DOCUMENTED D17/D74 contract (doc 12 §5.3); no real proxy, per-session CA, or TLS handshake (D50). The conforming control: zero violations expected."}}

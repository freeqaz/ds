{
  "_comment": "Synthetic VIOLATION (a): an onboarding-path permission requested ABOVE read level. contents:read is promoted to an 'admin' tier on the onboarding flow — above the read level the onboarding CI-read scope must stay within. Must FAIL with the named class onboarding-path-above-read-level.",
  "requested": [
    { "permission": "contents:read", "level": "admin", "flow": "onboarding CI-run-metadata read" },
    { "permission": "actions:read",  "level": "read",  "flow": "onboarding CI-run-metadata read" },
    { "permission": "metadata:read", "level": "read",  "flow": "onboarding CI-run-metadata read" }
  ]
}

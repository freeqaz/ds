{"ds_fixture":{"provenance":"synthetic","seam":"tlsproxyinspect TLS-3 explicit CONNECT-path git-push credential-swap golden-trace replay (doc 06 §2.2 + §3(c); doc 09 §5 TLS-3)","created":"2026-06-13","note":"Generated by the conformance test, not captured: client_npm_git_connect_test.go builds this trace IN-PROCESS from the canonical gitPushExchange() (host github.com used only as a request-line/SNI label, fixed ls-remote advertisement + receive-pack data) over one CONNECT tunnel, running the REAL credential-swap executor, and replays it byte-identically as the no-regression assert; (re)written only on the DS_TLS_GOLDEN_RECORD=1 record pass. Pack data pinned by digest+len; the swap rung carries the long-lived upstream FINGERPRINT (fp-long-github) as the swap proof — NEVER a credential value (TestCONNECT_GoldenFixturesNoSecretPayload asserts no short/long-lived credential byte is on disk). No real upstream (D50)."}}

// WriterRelay — the D137 browser writer-seat WRITE leg (sessions/10 §2.1).
//
// Package home: dreamserpent.attach.v1 (the SAME frozen package as the read
// schema — D137 maintainer amendment: the write path EXTENDS attach.v1 IN PLACE,
// additively, NOT a new attach.v2 package; Options A/B of sessions/10 §2.1/§2.2
// are SUPERSEDED). This file adds the one client→session input path D61 always
// promised but left structurally absent: a single, server-arbitrated WRITER
// seat the browser/canvas drives Claude Code through. It reopens the attach.v1
// one-shot freeze under EXPLICIT pre-ship authorization (D24/D137) — the change
// is WIRE-ADDITIVE (a new service + new messages on a flipped package; the
// `buf breaking --against` the prior baseline passes), so the orchestrator.v1 /
// hypervisor.v1 / canvas.v1 importers are UNAFFECTED and no v1 read message's
// existing fields/numbers move.
//
// THE READ LEG IS UNCHANGED. Spectators and the driver both still WATCH via the
// v1 SessionEvent stream (events.proto). The driver ADDITIONALLY holds an open
// DriveSession stream. Accepting a DriveInput causes the orchestrator to (a)
// write to Claude Code's stdin via the host-agent relay (the RELAY endpoint
// transport, attach_handle.proto), and (b) emit the ALREADY-RESERVED v1
// events.proto InputActivity{writer_seat_id, at, kind, seq} on the read stream
// — so every spectator sees that the driver typed and the D78 attendedness
// clock advances, WITH NO INPUT PAYLOAD ON THE READ LEG (the InputActivity
// "carries no input payload by default" rule, events.proto §6.1 row 7). The
// input *body* rides ONLY this write leg (DriveInput.payload), never the read
// schema. This file therefore REUSES the v1 InputActivity as the read-leg
// projection and DOES NOT redefine it.
//
// INVARIANTS (sessions/10 §3, D61/D78):
//   - One-writer/N-reader (D61): exactly one writer_seat_id is live per session;
//     it is a field on the SESSION RECORD (doc 15 §5.4). RequestWriterSeat is
//     server-arbitrated at the single WatchSession terminator; a second
//     concurrent writer is STRUCTURALLY impossible, not policy-forbidden —
//     DriveSession rejects any DriveInput whose writer_seat_id ≠ the live grant.
//   - Attributed record mutation (doc 15 §5.4 / D8 / D55): a grant/steal/yield
//     carries the driver's D55 human identity and is observable by all N readers
//     at granted_seq on the read stream. A steal cannot be silent.
//   - No-inject re-green (D137): a ROLE_READER holds only an attach.v1
//     AttachHandle{role=ROLE_READER} + a v1 WatchSession subscription — NO
//     WriterRelay grant, NO DriveSession stream. The ONLY path to stdin is
//     DriveSession behind a server-granted WriterSeatGrant behind D22/D55 auth.
//
// A message or field missed at this freeze-reopen is a v2-package event, not a
// patch (proto/FREEZE.md change control). Every message + enum carries a
// generous reserved tail so a later additive field stays additive (one-shot
// freeze discipline). buf lint STANDARD: every RPC request/response is named
// <Method>Request/<Method>Response, every enum has an _UNSPECIFIED zero value.

// Code generated by protoc-gen-go-grpc. DO NOT EDIT.
// versions:
// - protoc-gen-go-grpc v1.5.1
// - protoc             (unknown)
// source: dreamserpent/attach/v1/writer_relay.proto

package attachv1

import (
	context "context"
	grpc "google.golang.org/grpc"
	codes "google.golang.org/grpc/codes"
	status "google.golang.org/grpc/status"
)

// This is a compile-time assertion to ensure that this generated file
// is compatible with the grpc package it is being compiled against.
// Requires gRPC-Go v1.64.0 or later.
const _ = grpc.SupportPackageIsVersion9

const (
	WriterRelayService_RequestWriterSeat_FullMethodName = "/dreamserpent.attach.v1.WriterRelayService/RequestWriterSeat"
	WriterRelayService_YieldWriterSeat_FullMethodName   = "/dreamserpent.attach.v1.WriterRelayService/YieldWriterSeat"
	WriterRelayService_DriveSession_FullMethodName      = "/dreamserpent.attach.v1.WriterRelayService/DriveSession"
)

// WriterRelayServiceClient is the client API for WriterRelayService service.
//
// For semantics around ctx use and closing/ending streaming RPCs, please refer to https://pkg.go.dev/google.golang.org/grpc/?tab=doc#ClientConn.NewStream.
//
// =============================================================================
// WriterRelayService — the server-arbitrated write-leg service (the seam named
// "WriterRelay" in D137/sessions/10 §2.1; the `Service` suffix is the proto
// house-style rendering buf STANDARD's SERVICE_SUFFIX requires, seam identity
// unchanged — the hypervisor.v1 `HypervisorDriver`→`HypervisorDriverService` /
// identity.v1 `IdentityValidation`→`Service` precedent). Lives at the
// WatchSession terminator's owner (doc 15 §5.3): arbitration (RequestWriterSeat
// / YieldWriterSeat) and the drive verb (DriveSession) share ONE owner, so
// single-driver arbitration is one service's invariant, never split across two.
// =============================================================================
type WriterRelayServiceClient interface {
	// Take the one writer seat — the D61 handoff, a session-record mutation
	// (doc 15 §5.4). Server-arbitrated; exactly one live seat; a loser is refused.
	RequestWriterSeat(ctx context.Context, in *RequestWriterSeatRequest, opts ...grpc.CallOption) (*RequestWriterSeatResponse, error)
	// Release the held seat — the cooperative half of the handoff; observable.
	YieldWriterSeat(ctx context.Context, in *YieldWriterSeatRequest, opts ...grpc.CallOption) (*YieldWriterSeatResponse, error)
	// The drive channel — ONLY the live seat-holder may open it. Bidi: the client
	// streams DriveInput, the server streams acks (sessions/10 §2.1). Each
	// accepted input emits a v1 events.proto InputActivity on the read stream.
	DriveSession(ctx context.Context, opts ...grpc.CallOption) (grpc.BidiStreamingClient[DriveSessionRequest, DriveSessionResponse], error)
}

type writerRelayServiceClient struct {
	cc grpc.ClientConnInterface
}

func NewWriterRelayServiceClient(cc grpc.ClientConnInterface) WriterRelayServiceClient {
	return &writerRelayServiceClient{cc}
}

func (c *writerRelayServiceClient) RequestWriterSeat(ctx context.Context, in *RequestWriterSeatRequest, opts ...grpc.CallOption) (*RequestWriterSeatResponse, error) {
	cOpts := append([]grpc.CallOption{grpc.StaticMethod()}, opts...)
	out := new(RequestWriterSeatResponse)
	err := c.cc.Invoke(ctx, WriterRelayService_RequestWriterSeat_FullMethodName, in, out, cOpts...)
	if err != nil {
		return nil, err
	}
	return out, nil
}

func (c *writerRelayServiceClient) YieldWriterSeat(ctx context.Context, in *YieldWriterSeatRequest, opts ...grpc.CallOption) (*YieldWriterSeatResponse, error) {
	cOpts := append([]grpc.CallOption{grpc.StaticMethod()}, opts...)
	out := new(YieldWriterSeatResponse)
	err := c.cc.Invoke(ctx, WriterRelayService_YieldWriterSeat_FullMethodName, in, out, cOpts...)
	if err != nil {
		return nil, err
	}
	return out, nil
}

func (c *writerRelayServiceClient) DriveSession(ctx context.Context, opts ...grpc.CallOption) (grpc.BidiStreamingClient[DriveSessionRequest, DriveSessionResponse], error) {
	cOpts := append([]grpc.CallOption{grpc.StaticMethod()}, opts...)
	stream, err := c.cc.NewStream(ctx, &WriterRelayService_ServiceDesc.Streams[0], WriterRelayService_DriveSession_FullMethodName, cOpts...)
	if err != nil {
		return nil, err
	}
	x := &grpc.GenericClientStream[DriveSessionRequest, DriveSessionResponse]{ClientStream: stream}
	return x, nil
}

// This type alias is provided for backwards compatibility with existing code that references the prior non-generic stream type by name.
type WriterRelayService_DriveSessionClient = grpc.BidiStreamingClient[DriveSessionRequest, DriveSessionResponse]

// WriterRelayServiceServer is the server API for WriterRelayService service.
// All implementations must embed UnimplementedWriterRelayServiceServer
// for forward compatibility.
//
// =============================================================================
// WriterRelayService — the server-arbitrated write-leg service (the seam named
// "WriterRelay" in D137/sessions/10 §2.1; the `Service` suffix is the proto
// house-style rendering buf STANDARD's SERVICE_SUFFIX requires, seam identity
// unchanged — the hypervisor.v1 `HypervisorDriver`→`HypervisorDriverService` /
// identity.v1 `IdentityValidation`→`Service` precedent). Lives at the
// WatchSession terminator's owner (doc 15 §5.3): arbitration (RequestWriterSeat
// / YieldWriterSeat) and the drive verb (DriveSession) share ONE owner, so
// single-driver arbitration is one service's invariant, never split across two.
// =============================================================================
type WriterRelayServiceServer interface {
	// Take the one writer seat — the D61 handoff, a session-record mutation
	// (doc 15 §5.4). Server-arbitrated; exactly one live seat; a loser is refused.
	RequestWriterSeat(context.Context, *RequestWriterSeatRequest) (*RequestWriterSeatResponse, error)
	// Release the held seat — the cooperative half of the handoff; observable.
	YieldWriterSeat(context.Context, *YieldWriterSeatRequest) (*YieldWriterSeatResponse, error)
	// The drive channel — ONLY the live seat-holder may open it. Bidi: the client
	// streams DriveInput, the server streams acks (sessions/10 §2.1). Each
	// accepted input emits a v1 events.proto InputActivity on the read stream.
	DriveSession(grpc.BidiStreamingServer[DriveSessionRequest, DriveSessionResponse]) error
	mustEmbedUnimplementedWriterRelayServiceServer()
}

// UnimplementedWriterRelayServiceServer must be embedded to have
// forward compatible implementations.
//
// NOTE: this should be embedded by value instead of pointer to avoid a nil
// pointer dereference when methods are called.
type UnimplementedWriterRelayServiceServer struct{}

func (UnimplementedWriterRelayServiceServer) RequestWriterSeat(context.Context, *RequestWriterSeatRequest) (*RequestWriterSeatResponse, error) {
	return nil, status.Errorf(codes.Unimplemented, "method RequestWriterSeat not implemented")
}
func (UnimplementedWriterRelayServiceServer) YieldWriterSeat(context.Context, *YieldWriterSeatRequest) (*YieldWriterSeatResponse, error) {
	return nil, status.Errorf(codes.Unimplemented, "method YieldWriterSeat not implemented")
}
func (UnimplementedWriterRelayServiceServer) DriveSession(grpc.BidiStreamingServer[DriveSessionRequest, DriveSessionResponse]) error {
	return status.Errorf(codes.Unimplemented, "method DriveSession not implemented")
}
func (UnimplementedWriterRelayServiceServer) mustEmbedUnimplementedWriterRelayServiceServer() {}
func (UnimplementedWriterRelayServiceServer) testEmbeddedByValue()                            {}

// UnsafeWriterRelayServiceServer may be embedded to opt out of forward compatibility for this service.
// Use of this interface is not recommended, as added methods to WriterRelayServiceServer will
// result in compilation errors.
type UnsafeWriterRelayServiceServer interface {
	mustEmbedUnimplementedWriterRelayServiceServer()
}

func RegisterWriterRelayServiceServer(s grpc.ServiceRegistrar, srv WriterRelayServiceServer) {
	// If the following call pancis, it indicates UnimplementedWriterRelayServiceServer was
	// embedded by pointer and is nil.  This will cause panics if an
	// unimplemented method is ever invoked, so we test this at initialization
	// time to prevent it from happening at runtime later due to I/O.
	if t, ok := srv.(interface{ testEmbeddedByValue() }); ok {
		t.testEmbeddedByValue()
	}
	s.RegisterService(&WriterRelayService_ServiceDesc, srv)
}

func _WriterRelayService_RequestWriterSeat_Handler(srv interface{}, ctx context.Context, dec func(interface{}) error, interceptor grpc.UnaryServerInterceptor) (interface{}, error) {
	in := new(RequestWriterSeatRequest)
	if err := dec(in); err != nil {
		return nil, err
	}
	if interceptor == nil {
		return srv.(WriterRelayServiceServer).RequestWriterSeat(ctx, in)
	}
	info := &grpc.UnaryServerInfo{
		Server:     srv,
		FullMethod: WriterRelayService_RequestWriterSeat_FullMethodName,
	}
	handler := func(ctx context.Context, req interface{}) (interface{}, error) {
		return srv.(WriterRelayServiceServer).RequestWriterSeat(ctx, req.(*RequestWriterSeatRequest))
	}
	return interceptor(ctx, in, info, handler)
}

func _WriterRelayService_YieldWriterSeat_Handler(srv interface{}, ctx context.Context, dec func(interface{}) error, interceptor grpc.UnaryServerInterceptor) (interface{}, error) {
	in := new(YieldWriterSeatRequest)
	if err := dec(in); err != nil {
		return nil, err
	}
	if interceptor == nil {
		return srv.(WriterRelayServiceServer).YieldWriterSeat(ctx, in)
	}
	info := &grpc.UnaryServerInfo{
		Server:     srv,
		FullMethod: WriterRelayService_YieldWriterSeat_FullMethodName,
	}
	handler := func(ctx context.Context, req interface{}) (interface{}, error) {
		return srv.(WriterRelayServiceServer).YieldWriterSeat(ctx, req.(*YieldWriterSeatRequest))
	}
	return interceptor(ctx, in, info, handler)
}

func _WriterRelayService_DriveSession_Handler(srv interface{}, stream grpc.ServerStream) error {
	return srv.(WriterRelayServiceServer).DriveSession(&grpc.GenericServerStream[DriveSessionRequest, DriveSessionResponse]{ServerStream: stream})
}

// This type alias is provided for backwards compatibility with existing code that references the prior non-generic stream type by name.
type WriterRelayService_DriveSessionServer = grpc.BidiStreamingServer[DriveSessionRequest, DriveSessionResponse]

// WriterRelayService_ServiceDesc is the grpc.ServiceDesc for WriterRelayService service.
// It's only intended for direct use with grpc.RegisterService,
// and not to be introspected or modified (even as a copy)
var WriterRelayService_ServiceDesc = grpc.ServiceDesc{
	ServiceName: "dreamserpent.attach.v1.WriterRelayService",
	HandlerType: (*WriterRelayServiceServer)(nil),
	Methods: []grpc.MethodDesc{
		{
			MethodName: "RequestWriterSeat",
			Handler:    _WriterRelayService_RequestWriterSeat_Handler,
		},
		{
			MethodName: "YieldWriterSeat",
			Handler:    _WriterRelayService_YieldWriterSeat_Handler,
		},
	},
	Streams: []grpc.StreamDesc{
		{
			StreamName:    "DriveSession",
			Handler:       _WriterRelayService_DriveSession_Handler,
			ServerStreams: true,
			ClientStreams: true,
		},
	},
	Metadata: "dreamserpent/attach/v1/writer_relay.proto",
}

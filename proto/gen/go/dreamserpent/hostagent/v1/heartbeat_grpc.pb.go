// The host agent -> orchestrator reporting seam (doc 15 §5.2).
//
// A streaming heartbeat carries observed state, `applied_seq`, capacity, and
// per-session samples UP; the same seam carries the session-lifecycle data feed
// host-ward (digest ack relay, D22 grants, D78 attendedness — the D72-exempt
// class). Orchestrator <-> host agent is the contract harness's FIRST real seam
// (doc 15 §11, doc 06 §2.1).
//
// SHAPE freezes at M0; FIELD VALUES are rig-tuned (cadence strawman 5 s; 3
// missed beats => host presumed unreachable, sessions marked UNKNOWN, never
// auto-destroyed) — values are never frozen here (doc 15 §10). M0 freeze package
// (FREEZE.md `dreamserpent.hostagent.v1` row): bodies + baseline + flip land
// together; a slot missed at this one-shot freeze is a v2-package event. The
// package imports — never redefines — `hypervisor.v1.ObservedSession`
// (the shared observed-state element), so it freezes as one wave with or AFTER
// hypervisor.v1.

// Code generated by protoc-gen-go-grpc. DO NOT EDIT.
// versions:
// - protoc-gen-go-grpc v1.5.1
// - protoc             (unknown)
// source: dreamserpent/hostagent/v1/heartbeat.proto

package hostagentv1

import (
	context "context"
	grpc "google.golang.org/grpc"
	codes "google.golang.org/grpc/codes"
	status "google.golang.org/grpc/status"
)

// This is a compile-time assertion to ensure that this generated file
// is compatible with the grpc package it is being compiled against.
// Requires gRPC-Go v1.64.0 or later.
const _ = grpc.SupportPackageIsVersion9

const (
	HostAgentService_ReportHeartbeat_FullMethodName = "/dreamserpent.hostagent.v1.HostAgentService/ReportHeartbeat"
)

// HostAgentServiceClient is the client API for HostAgentService service.
//
// For semantics around ctx use and closing/ending streaming RPCs, please refer to https://pkg.go.dev/google.golang.org/grpc/?tab=doc#ClientConn.NewStream.
//
// HostAgentService is the host-agent reporting seam (doc 15 §5.2). The host agent
// streams Heartbeat UP; the orchestrator reconciler consumes it. Cadence and
// staleness are rig-tuned values, never frozen (doc 15 §10). The RPC takes a
// `<Verb>Request` / returns a `<Verb>Response` per buf STANDARD; the streamed
// request WRAPS the Heartbeat (so the meaningful message name is preserved while
// the stream frame stays additive-extensible).
type HostAgentServiceClient interface {
	// ReportHeartbeat is the client-streaming heartbeat: the host agent opens one
	// long-lived stream and emits a ReportHeartbeatRequest (carrying one Heartbeat)
	// per cadence interval. The response is returned when the stream closes
	// (graceful host drain) — steady-state observed state rides the stream, not the
	// response.
	ReportHeartbeat(ctx context.Context, opts ...grpc.CallOption) (grpc.ClientStreamingClient[ReportHeartbeatRequest, ReportHeartbeatResponse], error)
}

type hostAgentServiceClient struct {
	cc grpc.ClientConnInterface
}

func NewHostAgentServiceClient(cc grpc.ClientConnInterface) HostAgentServiceClient {
	return &hostAgentServiceClient{cc}
}

func (c *hostAgentServiceClient) ReportHeartbeat(ctx context.Context, opts ...grpc.CallOption) (grpc.ClientStreamingClient[ReportHeartbeatRequest, ReportHeartbeatResponse], error) {
	cOpts := append([]grpc.CallOption{grpc.StaticMethod()}, opts...)
	stream, err := c.cc.NewStream(ctx, &HostAgentService_ServiceDesc.Streams[0], HostAgentService_ReportHeartbeat_FullMethodName, cOpts...)
	if err != nil {
		return nil, err
	}
	x := &grpc.GenericClientStream[ReportHeartbeatRequest, ReportHeartbeatResponse]{ClientStream: stream}
	return x, nil
}

// This type alias is provided for backwards compatibility with existing code that references the prior non-generic stream type by name.
type HostAgentService_ReportHeartbeatClient = grpc.ClientStreamingClient[ReportHeartbeatRequest, ReportHeartbeatResponse]

// HostAgentServiceServer is the server API for HostAgentService service.
// All implementations must embed UnimplementedHostAgentServiceServer
// for forward compatibility.
//
// HostAgentService is the host-agent reporting seam (doc 15 §5.2). The host agent
// streams Heartbeat UP; the orchestrator reconciler consumes it. Cadence and
// staleness are rig-tuned values, never frozen (doc 15 §10). The RPC takes a
// `<Verb>Request` / returns a `<Verb>Response` per buf STANDARD; the streamed
// request WRAPS the Heartbeat (so the meaningful message name is preserved while
// the stream frame stays additive-extensible).
type HostAgentServiceServer interface {
	// ReportHeartbeat is the client-streaming heartbeat: the host agent opens one
	// long-lived stream and emits a ReportHeartbeatRequest (carrying one Heartbeat)
	// per cadence interval. The response is returned when the stream closes
	// (graceful host drain) — steady-state observed state rides the stream, not the
	// response.
	ReportHeartbeat(grpc.ClientStreamingServer[ReportHeartbeatRequest, ReportHeartbeatResponse]) error
	mustEmbedUnimplementedHostAgentServiceServer()
}

// UnimplementedHostAgentServiceServer must be embedded to have
// forward compatible implementations.
//
// NOTE: this should be embedded by value instead of pointer to avoid a nil
// pointer dereference when methods are called.
type UnimplementedHostAgentServiceServer struct{}

func (UnimplementedHostAgentServiceServer) ReportHeartbeat(grpc.ClientStreamingServer[ReportHeartbeatRequest, ReportHeartbeatResponse]) error {
	return status.Errorf(codes.Unimplemented, "method ReportHeartbeat not implemented")
}
func (UnimplementedHostAgentServiceServer) mustEmbedUnimplementedHostAgentServiceServer() {}
func (UnimplementedHostAgentServiceServer) testEmbeddedByValue()                          {}

// UnsafeHostAgentServiceServer may be embedded to opt out of forward compatibility for this service.
// Use of this interface is not recommended, as added methods to HostAgentServiceServer will
// result in compilation errors.
type UnsafeHostAgentServiceServer interface {
	mustEmbedUnimplementedHostAgentServiceServer()
}

func RegisterHostAgentServiceServer(s grpc.ServiceRegistrar, srv HostAgentServiceServer) {
	// If the following call pancis, it indicates UnimplementedHostAgentServiceServer was
	// embedded by pointer and is nil.  This will cause panics if an
	// unimplemented method is ever invoked, so we test this at initialization
	// time to prevent it from happening at runtime later due to I/O.
	if t, ok := srv.(interface{ testEmbeddedByValue() }); ok {
		t.testEmbeddedByValue()
	}
	s.RegisterService(&HostAgentService_ServiceDesc, srv)
}

func _HostAgentService_ReportHeartbeat_Handler(srv interface{}, stream grpc.ServerStream) error {
	return srv.(HostAgentServiceServer).ReportHeartbeat(&grpc.GenericServerStream[ReportHeartbeatRequest, ReportHeartbeatResponse]{ServerStream: stream})
}

// This type alias is provided for backwards compatibility with existing code that references the prior non-generic stream type by name.
type HostAgentService_ReportHeartbeatServer = grpc.ClientStreamingServer[ReportHeartbeatRequest, ReportHeartbeatResponse]

// HostAgentService_ServiceDesc is the grpc.ServiceDesc for HostAgentService service.
// It's only intended for direct use with grpc.RegisterService,
// and not to be introspected or modified (even as a copy)
var HostAgentService_ServiceDesc = grpc.ServiceDesc{
	ServiceName: "dreamserpent.hostagent.v1.HostAgentService",
	HandlerType: (*HostAgentServiceServer)(nil),
	Methods:     []grpc.MethodDesc{},
	Streams: []grpc.StreamDesc{
		{
			StreamName:    "ReportHeartbeat",
			Handler:       _HostAgentService_ReportHeartbeat_Handler,
			ClientStreams: true,
		},
	},
	Metadata: "dreamserpent/hostagent/v1/heartbeat.proto",
}

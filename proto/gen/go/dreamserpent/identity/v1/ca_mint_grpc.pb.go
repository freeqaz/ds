// The interception-CA mint seam (D17/D82) and the IdentityMint service surface
// (doc 16 §4 / §9).
//
// D82 ratifies that Identity's D22 mint service mints the per-session
// interception CA (D17), under TWO SEPARATE ROOT HIERARCHIES — workload-identity
// (hierarchy 1) and interception (hierarchy 2) — so compromise of one never
// yields the other's signing capability (D82; doc 16 §4). Both roots live
// off-host in the D39 secret-store trust zone; an interception-root signature
// can never validate as a workload identity, so the egress-gateway
// TLS-termination capability and the attribution capability fail independently.
//
// PLACEMENT (D22/D35/D82, reconciled — doc 16 §9): the `IdentityMint` service
// and its messages live in `dreamserpent.identity.v1`; `dreamserpent.orchestrator.v1`
// carries NO Mint RPC. D35's MintIdentity-as-separate-service is satisfied by
// this separate package + dedicated instance (doc 02 §7 / D39), with the
// orchestrator merely FRONTING the mint — an orchestrator/KVM compromise yields
// no signing keys.
//
// Stage-0 freeze seam: of `IdentityMint`, only `MintInterceptionCA` rode the
// Stage-0 flip. `MintGrants`, `MintWorkloadIdentity`, `RevokeSession`, and now
// `MintSessionToken` are all PROMOTED as additive-in-M0-window RPCs (ratified
// D111 — doc 16 §9; `MintSessionToken` is the doc 19 token work, D99/D97). No
// mint RPC remains reserved. Each promotion is buf-safe: a new service RPC + new
// messages on an already-frozen package, NOT a new freeze and NOT a break.

// Code generated by protoc-gen-go-grpc. DO NOT EDIT.
// versions:
// - protoc-gen-go-grpc v1.5.1
// - protoc             (unknown)
// source: dreamserpent/identity/v1/ca_mint.proto

package identityv1

import (
	context "context"
	grpc "google.golang.org/grpc"
	codes "google.golang.org/grpc/codes"
	status "google.golang.org/grpc/status"
)

// This is a compile-time assertion to ensure that this generated file
// is compatible with the grpc package it is being compiled against.
// Requires gRPC-Go v1.64.0 or later.
const _ = grpc.SupportPackageIsVersion9

const (
	IdentityMintService_MintInterceptionCA_FullMethodName   = "/dreamserpent.identity.v1.IdentityMintService/MintInterceptionCA"
	IdentityMintService_MintGrants_FullMethodName           = "/dreamserpent.identity.v1.IdentityMintService/MintGrants"
	IdentityMintService_MintWorkloadIdentity_FullMethodName = "/dreamserpent.identity.v1.IdentityMintService/MintWorkloadIdentity"
	IdentityMintService_RevokeSession_FullMethodName        = "/dreamserpent.identity.v1.IdentityMintService/RevokeSession"
	IdentityMintService_MintSessionToken_FullMethodName     = "/dreamserpent.identity.v1.IdentityMintService/MintSessionToken"
)

// IdentityMintServiceClient is the client API for IdentityMintService service.
//
// For semantics around ctx use and closing/ending streaming RPCs, please refer to https://pkg.go.dev/google.golang.org/grpc/?tab=doc#ClientConn.NewStream.
//
// IdentityMintService is the mint surface (doc 16 §4 `service IdentityMint`).
// The `IdentityMint` name in doc 16's prose sketch is rendered with the proto
// house-style `Service` suffix (buf STANDARD); the seam identity is unchanged.
//
// At Stage 0 this service exposed exactly one RPC — `MintInterceptionCA`. The
// M0-window additive promotions (D111) have since landed `MintGrants`,
// `MintWorkloadIdentity`, `RevokeSession`, and `MintSessionToken` as real RPCs;
// no mint RPC remains reserved. Each addition is an additive, buf-safe event,
// never a v2-package break (doc 16 §9, D111).
type IdentityMintServiceClient interface {
	// MintInterceptionCA mints the per-session interception CA under the
	// interception root (hierarchy 2, D82), distinct from the workload-identity
	// root (hierarchy 1).
	//
	// LIFECYCLE: minted at session create, dies at teardown. Per-session CA
	// material is session-lifecycle data — the D72-EXEMPT class, same as the D22
	// grants (doc 16 §4): session-scoped, not policy_log-sequenced. Session A's CA
	// is useless against session B (doc 16 §4 / §13 assurance row).
	//
	// KEY CUSTODY (D39): both roots live off-host in the secret-store trust zone.
	// The per-session interception-CA key is delivered proxy-bound to ds-tlsproxy
	// for on-the-fly per-origin leaf minting and is destroyed at teardown — the
	// bounded D76 exposure: a compromised proxy holds one live session's
	// interception material, never a root, never workload-identity signing.
	//
	// TRUST-STORE INJECTION: the orchestrator-driven provisioning hook inserts the
	// returned CA into the per-session CoW overlay between CloneFromImage and
	// entrypoint, fail-closed — a session never starts with a missing/empty trust
	// anchor (D82; doc 16 §4). The master session-create choreography is the
	// orchestrator doc's; this seam owns only the mint.
	MintInterceptionCA(ctx context.Context, in *MintInterceptionCARequest, opts ...grpc.CallOption) (*MintInterceptionCAResponse, error)
	// MintGrants issues the deterministic grant set (doc 16 §5.1, §9; ratified
	// D111 — the additive grant-record promotion in the M0 window). It intersects
	// the session's env spec (D7) with the org `services[]` registry and returns a
	// `GrantSet` of typed grant records (identity × service × scope × TTL) plus the
	// per-service placeholder tokens the agent holds. The decision is a
	// DETERMINISTIC LOOKUP, never a policy evaluation — no Cedar, no expression
	// language in v0 (D52). The request/response messages live in grants.proto in
	// this package. This is the additive promotion of the former reserved
	// `rpc MintGrants(...) returns (...)  // GrantSet: typed records` comment, made
	// a real buf-safe service/RPC addition to the already-frozen package — NOT a
	// new freeze, NOT a break (FREEZE.md identity.v1 row). The grant-FETCH protocol
	// and D55 Vault integration that consume the returned `grant_ref` are a
	// SEPARATE later rider (doc 16 §9 grant-fetch / D84 rows; M1 window).
	//
	// The response is the doc 16 §5.1 `GrantSet` (NOT MintGrantsResponse): the
	// name is load-bearing — it is the §9 / reserved-comment vocabulary ("GrantSet:
	// typed records") and the tested Go type `mint.GrantSet` this promotion mirrors
	// (grants.proto), so a later Go repoint stays mechanical. buf STANDARD's
	// RPC_RESPONSE_STANDARD_NAME is ignored on this one line for that reason — the
	// same allowance the FROZEN auth.v1 package takes for `AuthTokenResponse`.
	// buf:lint:ignore RPC_RESPONSE_STANDARD_NAME
	MintGrants(ctx context.Context, in *MintGrantsRequest, opts ...grpc.CallOption) (*GrantSet, error)
	// MintWorkloadIdentity mints hierarchy-1 workload identity (doc 16 §3.1;
	// ratified D111 — the additive M0-window promotion). The response is an X.509
	// leaf with a SPIFFE-compatible URI SAN (`spiffe://<org>/session/<uuid>`)
	// chained to the workload-identity ROOT (hierarchy 1, D82 — structurally
	// distinct from the interception root MintInterceptionCA chains to), PLUS a
	// parallel JWT presentation carrying the §3.1 claim set (`session_uuid`,
	// `launching_user`, `org`, `repo/branch`, `runtime`, `parent_session`, and the
	// RESERVED `service_principal` marker), TTL = session lifetime. SPIFFE-
	// compatible naming now is what makes the M3 SPIRE migration a pure substrate
	// swap (doc 16 §3.1). The mint also registers/refreshes the session record so
	// the workload key is on the D22 Validate path and the session is live.
	//
	// This is the additive promotion of the former reserved
	// `rpc MintWorkloadIdentity(...) returns (...)` comment to a real buf-safe
	// service/RPC + message addition on the already-frozen package — NOT a new
	// freeze, NOT a break (FREEZE.md identity.v1 row). It mirrors the tested native
	// `mint.Shim.MintWorkloadIdentity` (identity/mint/mint.go) field-for-field so
	// a later Go repoint is mechanical (the M0 fakes-first pattern, doc 05 OQ3).
	MintWorkloadIdentity(ctx context.Context, in *MintWorkloadIdentityRequest, opts ...grpc.CallOption) (*MintWorkloadIdentityResponse, error)
	// RevokeSession marks the session record revoked (doc 16 §5.4; ratified D111 —
	// the additive M0-window promotion). The minimal CA ships NO CRL/OCSP: this is
	// TTL-as-revocation plus ACTIVE EVICTION — on suspend/kill/admin-revoke the
	// session record is marked and the D22 `Validate` session-liveness check then
	// fails CLOSED for that session (verdict DENY with a machine-readable reason in
	// the D77 in-band-403 shape), so a stolen still-valid credential fails
	// immediately (doc 16 §5.4). Revoking an unknown session creates a tombstone so
	// a later mint/validate races CLOSED, not open. The ack is an idempotent
	// acknowledgement, never a transport error.
	//
	// This is the additive promotion of the former reserved
	// `rpc RevokeSession(...) returns (...)` comment to a real buf-safe service/RPC
	// + message addition on the already-frozen package — NOT a new freeze, NOT a
	// break (FREEZE.md identity.v1 row). It mirrors the tested native
	// `mint.Shim.RevokeSession` (identity/mint/mint.go) so a later Go repoint is
	// mechanical (the M0 fakes-first pattern, doc 05 OQ3).
	RevokeSession(ctx context.Context, in *RevokeSessionRequest, opts ...grpc.CallOption) (*RevokeSessionResponse, error)
	// MintSessionToken mints the scoped per-session BASE token (doc 19 §3; D99/
	// D97 — the additive M0-window promotion, ratified D111). The token is the
	// format-opaque presented credential (a serialized Biscuit under the default
	// substrate, D98) that rides the existing D22 `Validate` seam's
	// `presented_credential` UNCHANGED (doc 19 §5) — neither a cert pool nor the
	// workload JWS verifier can even parse it (the §13 structural separation: a
	// session-token signature must NEVER validate as workload identity nor as
	// interception material, the THIRD signing context, D99). The base token always
	// has an EMPTY `parent_session` (doc 19 §3); child hops come from offline
	// `AttenuateSessionToken` (doc 19 §4: zero mint RPCs as the tree widens), NEVER
	// a second mint — so attenuation is deliberately NOT an RPC here. The mint also
	// registers/refreshes the session record so the existing `Validate` seam
	// re-checks session liveness on every presentation (doc 16 §5.4 / doc 19 §7).
	//
	// This is the additive promotion of the former reserved
	// `rpc MintSessionToken(...) returns (...)` comment to a real buf-safe
	// service/RPC + message addition on the already-frozen package — NOT a new
	// freeze, NOT a break (FREEZE.md identity.v1 row). It mirrors the tested native
	// `mint.Shim.MintSessionToken` (identity/mint/sessiontoken.go) field-for-field
	// (`MintSessionTokenReq` → request, `SessionTokenBundle` → response) so a later
	// Go repoint is mechanical (the M0 fakes-first pattern, doc 05 OQ3).
	MintSessionToken(ctx context.Context, in *MintSessionTokenRequest, opts ...grpc.CallOption) (*MintSessionTokenResponse, error)
}

type identityMintServiceClient struct {
	cc grpc.ClientConnInterface
}

func NewIdentityMintServiceClient(cc grpc.ClientConnInterface) IdentityMintServiceClient {
	return &identityMintServiceClient{cc}
}

func (c *identityMintServiceClient) MintInterceptionCA(ctx context.Context, in *MintInterceptionCARequest, opts ...grpc.CallOption) (*MintInterceptionCAResponse, error) {
	cOpts := append([]grpc.CallOption{grpc.StaticMethod()}, opts...)
	out := new(MintInterceptionCAResponse)
	err := c.cc.Invoke(ctx, IdentityMintService_MintInterceptionCA_FullMethodName, in, out, cOpts...)
	if err != nil {
		return nil, err
	}
	return out, nil
}

func (c *identityMintServiceClient) MintGrants(ctx context.Context, in *MintGrantsRequest, opts ...grpc.CallOption) (*GrantSet, error) {
	cOpts := append([]grpc.CallOption{grpc.StaticMethod()}, opts...)
	out := new(GrantSet)
	err := c.cc.Invoke(ctx, IdentityMintService_MintGrants_FullMethodName, in, out, cOpts...)
	if err != nil {
		return nil, err
	}
	return out, nil
}

func (c *identityMintServiceClient) MintWorkloadIdentity(ctx context.Context, in *MintWorkloadIdentityRequest, opts ...grpc.CallOption) (*MintWorkloadIdentityResponse, error) {
	cOpts := append([]grpc.CallOption{grpc.StaticMethod()}, opts...)
	out := new(MintWorkloadIdentityResponse)
	err := c.cc.Invoke(ctx, IdentityMintService_MintWorkloadIdentity_FullMethodName, in, out, cOpts...)
	if err != nil {
		return nil, err
	}
	return out, nil
}

func (c *identityMintServiceClient) RevokeSession(ctx context.Context, in *RevokeSessionRequest, opts ...grpc.CallOption) (*RevokeSessionResponse, error) {
	cOpts := append([]grpc.CallOption{grpc.StaticMethod()}, opts...)
	out := new(RevokeSessionResponse)
	err := c.cc.Invoke(ctx, IdentityMintService_RevokeSession_FullMethodName, in, out, cOpts...)
	if err != nil {
		return nil, err
	}
	return out, nil
}

func (c *identityMintServiceClient) MintSessionToken(ctx context.Context, in *MintSessionTokenRequest, opts ...grpc.CallOption) (*MintSessionTokenResponse, error) {
	cOpts := append([]grpc.CallOption{grpc.StaticMethod()}, opts...)
	out := new(MintSessionTokenResponse)
	err := c.cc.Invoke(ctx, IdentityMintService_MintSessionToken_FullMethodName, in, out, cOpts...)
	if err != nil {
		return nil, err
	}
	return out, nil
}

// IdentityMintServiceServer is the server API for IdentityMintService service.
// All implementations must embed UnimplementedIdentityMintServiceServer
// for forward compatibility.
//
// IdentityMintService is the mint surface (doc 16 §4 `service IdentityMint`).
// The `IdentityMint` name in doc 16's prose sketch is rendered with the proto
// house-style `Service` suffix (buf STANDARD); the seam identity is unchanged.
//
// At Stage 0 this service exposed exactly one RPC — `MintInterceptionCA`. The
// M0-window additive promotions (D111) have since landed `MintGrants`,
// `MintWorkloadIdentity`, `RevokeSession`, and `MintSessionToken` as real RPCs;
// no mint RPC remains reserved. Each addition is an additive, buf-safe event,
// never a v2-package break (doc 16 §9, D111).
type IdentityMintServiceServer interface {
	// MintInterceptionCA mints the per-session interception CA under the
	// interception root (hierarchy 2, D82), distinct from the workload-identity
	// root (hierarchy 1).
	//
	// LIFECYCLE: minted at session create, dies at teardown. Per-session CA
	// material is session-lifecycle data — the D72-EXEMPT class, same as the D22
	// grants (doc 16 §4): session-scoped, not policy_log-sequenced. Session A's CA
	// is useless against session B (doc 16 §4 / §13 assurance row).
	//
	// KEY CUSTODY (D39): both roots live off-host in the secret-store trust zone.
	// The per-session interception-CA key is delivered proxy-bound to ds-tlsproxy
	// for on-the-fly per-origin leaf minting and is destroyed at teardown — the
	// bounded D76 exposure: a compromised proxy holds one live session's
	// interception material, never a root, never workload-identity signing.
	//
	// TRUST-STORE INJECTION: the orchestrator-driven provisioning hook inserts the
	// returned CA into the per-session CoW overlay between CloneFromImage and
	// entrypoint, fail-closed — a session never starts with a missing/empty trust
	// anchor (D82; doc 16 §4). The master session-create choreography is the
	// orchestrator doc's; this seam owns only the mint.
	MintInterceptionCA(context.Context, *MintInterceptionCARequest) (*MintInterceptionCAResponse, error)
	// MintGrants issues the deterministic grant set (doc 16 §5.1, §9; ratified
	// D111 — the additive grant-record promotion in the M0 window). It intersects
	// the session's env spec (D7) with the org `services[]` registry and returns a
	// `GrantSet` of typed grant records (identity × service × scope × TTL) plus the
	// per-service placeholder tokens the agent holds. The decision is a
	// DETERMINISTIC LOOKUP, never a policy evaluation — no Cedar, no expression
	// language in v0 (D52). The request/response messages live in grants.proto in
	// this package. This is the additive promotion of the former reserved
	// `rpc MintGrants(...) returns (...)  // GrantSet: typed records` comment, made
	// a real buf-safe service/RPC addition to the already-frozen package — NOT a
	// new freeze, NOT a break (FREEZE.md identity.v1 row). The grant-FETCH protocol
	// and D55 Vault integration that consume the returned `grant_ref` are a
	// SEPARATE later rider (doc 16 §9 grant-fetch / D84 rows; M1 window).
	//
	// The response is the doc 16 §5.1 `GrantSet` (NOT MintGrantsResponse): the
	// name is load-bearing — it is the §9 / reserved-comment vocabulary ("GrantSet:
	// typed records") and the tested Go type `mint.GrantSet` this promotion mirrors
	// (grants.proto), so a later Go repoint stays mechanical. buf STANDARD's
	// RPC_RESPONSE_STANDARD_NAME is ignored on this one line for that reason — the
	// same allowance the FROZEN auth.v1 package takes for `AuthTokenResponse`.
	// buf:lint:ignore RPC_RESPONSE_STANDARD_NAME
	MintGrants(context.Context, *MintGrantsRequest) (*GrantSet, error)
	// MintWorkloadIdentity mints hierarchy-1 workload identity (doc 16 §3.1;
	// ratified D111 — the additive M0-window promotion). The response is an X.509
	// leaf with a SPIFFE-compatible URI SAN (`spiffe://<org>/session/<uuid>`)
	// chained to the workload-identity ROOT (hierarchy 1, D82 — structurally
	// distinct from the interception root MintInterceptionCA chains to), PLUS a
	// parallel JWT presentation carrying the §3.1 claim set (`session_uuid`,
	// `launching_user`, `org`, `repo/branch`, `runtime`, `parent_session`, and the
	// RESERVED `service_principal` marker), TTL = session lifetime. SPIFFE-
	// compatible naming now is what makes the M3 SPIRE migration a pure substrate
	// swap (doc 16 §3.1). The mint also registers/refreshes the session record so
	// the workload key is on the D22 Validate path and the session is live.
	//
	// This is the additive promotion of the former reserved
	// `rpc MintWorkloadIdentity(...) returns (...)` comment to a real buf-safe
	// service/RPC + message addition on the already-frozen package — NOT a new
	// freeze, NOT a break (FREEZE.md identity.v1 row). It mirrors the tested native
	// `mint.Shim.MintWorkloadIdentity` (identity/mint/mint.go) field-for-field so
	// a later Go repoint is mechanical (the M0 fakes-first pattern, doc 05 OQ3).
	MintWorkloadIdentity(context.Context, *MintWorkloadIdentityRequest) (*MintWorkloadIdentityResponse, error)
	// RevokeSession marks the session record revoked (doc 16 §5.4; ratified D111 —
	// the additive M0-window promotion). The minimal CA ships NO CRL/OCSP: this is
	// TTL-as-revocation plus ACTIVE EVICTION — on suspend/kill/admin-revoke the
	// session record is marked and the D22 `Validate` session-liveness check then
	// fails CLOSED for that session (verdict DENY with a machine-readable reason in
	// the D77 in-band-403 shape), so a stolen still-valid credential fails
	// immediately (doc 16 §5.4). Revoking an unknown session creates a tombstone so
	// a later mint/validate races CLOSED, not open. The ack is an idempotent
	// acknowledgement, never a transport error.
	//
	// This is the additive promotion of the former reserved
	// `rpc RevokeSession(...) returns (...)` comment to a real buf-safe service/RPC
	// + message addition on the already-frozen package — NOT a new freeze, NOT a
	// break (FREEZE.md identity.v1 row). It mirrors the tested native
	// `mint.Shim.RevokeSession` (identity/mint/mint.go) so a later Go repoint is
	// mechanical (the M0 fakes-first pattern, doc 05 OQ3).
	RevokeSession(context.Context, *RevokeSessionRequest) (*RevokeSessionResponse, error)
	// MintSessionToken mints the scoped per-session BASE token (doc 19 §3; D99/
	// D97 — the additive M0-window promotion, ratified D111). The token is the
	// format-opaque presented credential (a serialized Biscuit under the default
	// substrate, D98) that rides the existing D22 `Validate` seam's
	// `presented_credential` UNCHANGED (doc 19 §5) — neither a cert pool nor the
	// workload JWS verifier can even parse it (the §13 structural separation: a
	// session-token signature must NEVER validate as workload identity nor as
	// interception material, the THIRD signing context, D99). The base token always
	// has an EMPTY `parent_session` (doc 19 §3); child hops come from offline
	// `AttenuateSessionToken` (doc 19 §4: zero mint RPCs as the tree widens), NEVER
	// a second mint — so attenuation is deliberately NOT an RPC here. The mint also
	// registers/refreshes the session record so the existing `Validate` seam
	// re-checks session liveness on every presentation (doc 16 §5.4 / doc 19 §7).
	//
	// This is the additive promotion of the former reserved
	// `rpc MintSessionToken(...) returns (...)` comment to a real buf-safe
	// service/RPC + message addition on the already-frozen package — NOT a new
	// freeze, NOT a break (FREEZE.md identity.v1 row). It mirrors the tested native
	// `mint.Shim.MintSessionToken` (identity/mint/sessiontoken.go) field-for-field
	// (`MintSessionTokenReq` → request, `SessionTokenBundle` → response) so a later
	// Go repoint is mechanical (the M0 fakes-first pattern, doc 05 OQ3).
	MintSessionToken(context.Context, *MintSessionTokenRequest) (*MintSessionTokenResponse, error)
	mustEmbedUnimplementedIdentityMintServiceServer()
}

// UnimplementedIdentityMintServiceServer must be embedded to have
// forward compatible implementations.
//
// NOTE: this should be embedded by value instead of pointer to avoid a nil
// pointer dereference when methods are called.
type UnimplementedIdentityMintServiceServer struct{}

func (UnimplementedIdentityMintServiceServer) MintInterceptionCA(context.Context, *MintInterceptionCARequest) (*MintInterceptionCAResponse, error) {
	return nil, status.Errorf(codes.Unimplemented, "method MintInterceptionCA not implemented")
}
func (UnimplementedIdentityMintServiceServer) MintGrants(context.Context, *MintGrantsRequest) (*GrantSet, error) {
	return nil, status.Errorf(codes.Unimplemented, "method MintGrants not implemented")
}
func (UnimplementedIdentityMintServiceServer) MintWorkloadIdentity(context.Context, *MintWorkloadIdentityRequest) (*MintWorkloadIdentityResponse, error) {
	return nil, status.Errorf(codes.Unimplemented, "method MintWorkloadIdentity not implemented")
}
func (UnimplementedIdentityMintServiceServer) RevokeSession(context.Context, *RevokeSessionRequest) (*RevokeSessionResponse, error) {
	return nil, status.Errorf(codes.Unimplemented, "method RevokeSession not implemented")
}
func (UnimplementedIdentityMintServiceServer) MintSessionToken(context.Context, *MintSessionTokenRequest) (*MintSessionTokenResponse, error) {
	return nil, status.Errorf(codes.Unimplemented, "method MintSessionToken not implemented")
}
func (UnimplementedIdentityMintServiceServer) mustEmbedUnimplementedIdentityMintServiceServer() {}
func (UnimplementedIdentityMintServiceServer) testEmbeddedByValue()                             {}

// UnsafeIdentityMintServiceServer may be embedded to opt out of forward compatibility for this service.
// Use of this interface is not recommended, as added methods to IdentityMintServiceServer will
// result in compilation errors.
type UnsafeIdentityMintServiceServer interface {
	mustEmbedUnimplementedIdentityMintServiceServer()
}

func RegisterIdentityMintServiceServer(s grpc.ServiceRegistrar, srv IdentityMintServiceServer) {
	// If the following call pancis, it indicates UnimplementedIdentityMintServiceServer was
	// embedded by pointer and is nil.  This will cause panics if an
	// unimplemented method is ever invoked, so we test this at initialization
	// time to prevent it from happening at runtime later due to I/O.
	if t, ok := srv.(interface{ testEmbeddedByValue() }); ok {
		t.testEmbeddedByValue()
	}
	s.RegisterService(&IdentityMintService_ServiceDesc, srv)
}

func _IdentityMintService_MintInterceptionCA_Handler(srv interface{}, ctx context.Context, dec func(interface{}) error, interceptor grpc.UnaryServerInterceptor) (interface{}, error) {
	in := new(MintInterceptionCARequest)
	if err := dec(in); err != nil {
		return nil, err
	}
	if interceptor == nil {
		return srv.(IdentityMintServiceServer).MintInterceptionCA(ctx, in)
	}
	info := &grpc.UnaryServerInfo{
		Server:     srv,
		FullMethod: IdentityMintService_MintInterceptionCA_FullMethodName,
	}
	handler := func(ctx context.Context, req interface{}) (interface{}, error) {
		return srv.(IdentityMintServiceServer).MintInterceptionCA(ctx, req.(*MintInterceptionCARequest))
	}
	return interceptor(ctx, in, info, handler)
}

func _IdentityMintService_MintGrants_Handler(srv interface{}, ctx context.Context, dec func(interface{}) error, interceptor grpc.UnaryServerInterceptor) (interface{}, error) {
	in := new(MintGrantsRequest)
	if err := dec(in); err != nil {
		return nil, err
	}
	if interceptor == nil {
		return srv.(IdentityMintServiceServer).MintGrants(ctx, in)
	}
	info := &grpc.UnaryServerInfo{
		Server:     srv,
		FullMethod: IdentityMintService_MintGrants_FullMethodName,
	}
	handler := func(ctx context.Context, req interface{}) (interface{}, error) {
		return srv.(IdentityMintServiceServer).MintGrants(ctx, req.(*MintGrantsRequest))
	}
	return interceptor(ctx, in, info, handler)
}

func _IdentityMintService_MintWorkloadIdentity_Handler(srv interface{}, ctx context.Context, dec func(interface{}) error, interceptor grpc.UnaryServerInterceptor) (interface{}, error) {
	in := new(MintWorkloadIdentityRequest)
	if err := dec(in); err != nil {
		return nil, err
	}
	if interceptor == nil {
		return srv.(IdentityMintServiceServer).MintWorkloadIdentity(ctx, in)
	}
	info := &grpc.UnaryServerInfo{
		Server:     srv,
		FullMethod: IdentityMintService_MintWorkloadIdentity_FullMethodName,
	}
	handler := func(ctx context.Context, req interface{}) (interface{}, error) {
		return srv.(IdentityMintServiceServer).MintWorkloadIdentity(ctx, req.(*MintWorkloadIdentityRequest))
	}
	return interceptor(ctx, in, info, handler)
}

func _IdentityMintService_RevokeSession_Handler(srv interface{}, ctx context.Context, dec func(interface{}) error, interceptor grpc.UnaryServerInterceptor) (interface{}, error) {
	in := new(RevokeSessionRequest)
	if err := dec(in); err != nil {
		return nil, err
	}
	if interceptor == nil {
		return srv.(IdentityMintServiceServer).RevokeSession(ctx, in)
	}
	info := &grpc.UnaryServerInfo{
		Server:     srv,
		FullMethod: IdentityMintService_RevokeSession_FullMethodName,
	}
	handler := func(ctx context.Context, req interface{}) (interface{}, error) {
		return srv.(IdentityMintServiceServer).RevokeSession(ctx, req.(*RevokeSessionRequest))
	}
	return interceptor(ctx, in, info, handler)
}

func _IdentityMintService_MintSessionToken_Handler(srv interface{}, ctx context.Context, dec func(interface{}) error, interceptor grpc.UnaryServerInterceptor) (interface{}, error) {
	in := new(MintSessionTokenRequest)
	if err := dec(in); err != nil {
		return nil, err
	}
	if interceptor == nil {
		return srv.(IdentityMintServiceServer).MintSessionToken(ctx, in)
	}
	info := &grpc.UnaryServerInfo{
		Server:     srv,
		FullMethod: IdentityMintService_MintSessionToken_FullMethodName,
	}
	handler := func(ctx context.Context, req interface{}) (interface{}, error) {
		return srv.(IdentityMintServiceServer).MintSessionToken(ctx, req.(*MintSessionTokenRequest))
	}
	return interceptor(ctx, in, info, handler)
}

// IdentityMintService_ServiceDesc is the grpc.ServiceDesc for IdentityMintService service.
// It's only intended for direct use with grpc.RegisterService,
// and not to be introspected or modified (even as a copy)
var IdentityMintService_ServiceDesc = grpc.ServiceDesc{
	ServiceName: "dreamserpent.identity.v1.IdentityMintService",
	HandlerType: (*IdentityMintServiceServer)(nil),
	Methods: []grpc.MethodDesc{
		{
			MethodName: "MintInterceptionCA",
			Handler:    _IdentityMintService_MintInterceptionCA_Handler,
		},
		{
			MethodName: "MintGrants",
			Handler:    _IdentityMintService_MintGrants_Handler,
		},
		{
			MethodName: "MintWorkloadIdentity",
			Handler:    _IdentityMintService_MintWorkloadIdentity_Handler,
		},
		{
			MethodName: "RevokeSession",
			Handler:    _IdentityMintService_RevokeSession_Handler,
		},
		{
			MethodName: "MintSessionToken",
			Handler:    _IdentityMintService_MintSessionToken_Handler,
		},
	},
	Streams:  []grpc.StreamDesc{},
	Metadata: "dreamserpent/identity/v1/ca_mint.proto",
}

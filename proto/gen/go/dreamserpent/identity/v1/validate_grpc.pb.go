// The D22 identity-validation seam (doc 16 §4 / §9).
//
// One frozen `Validate` contract is what makes the Identity substrate
// progression — M0 throwaway shim -> M1 own minimal CA -> M3 SPIFFE/SPIRE — a
// substrate swap behind a frozen contract, not a rebuild (D22; doc 05 §7 edge
// 5). Every substrate plus customer-IdP-backed identities satisfies this exact
// D24-versioned contract.
//
// Stage-0 freeze seam (doc 16 §9, FREEZE.md `dreamserpent.identity.v1` row).

// Code generated by protoc-gen-go-grpc. DO NOT EDIT.
// versions:
// - protoc-gen-go-grpc v1.5.1
// - protoc             (unknown)
// source: dreamserpent/identity/v1/validate.proto

package identityv1

import (
	context "context"
	grpc "google.golang.org/grpc"
	codes "google.golang.org/grpc/codes"
	status "google.golang.org/grpc/status"
)

// This is a compile-time assertion to ensure that this generated file
// is compatible with the grpc package it is being compiled against.
// Requires gRPC-Go v1.64.0 or later.
const _ = grpc.SupportPackageIsVersion9

const (
	IdentityValidationService_Validate_FullMethodName = "/dreamserpent.identity.v1.IdentityValidationService/Validate"
)

// IdentityValidationServiceClient is the client API for IdentityValidationService service.
//
// For semantics around ctx use and closing/ending streaming RPCs, please refer to https://pkg.go.dev/google.golang.org/grpc/?tab=doc#ClientConn.NewStream.
//
// IdentityValidationService is the D22 sidecar seam, consumed by ds-tlsproxy on
// the TLS-5 swap path (doc 16 §4 / §9). The `IdentityValidation` service name in
// doc 16's prose sketch is rendered here with the proto house-style `Service`
// suffix (buf STANDARD); the seam identity is the D22 contract, unchanged.
//
// SYNC, session-liveness semantics: a single in-line call on the swap path.
// `Validate` checks signature + freshness + SESSION LIVENESS + grant lookup
// (doc 16 §4). The session-liveness check is what makes a stolen-but-unexpired
// credential fail immediately on suspend/kill/admin-revoke — the minimal CA
// ships no CRL/OCSP, so liveness, not revocation, is the catch (doc 16 §5.4).
//
// SWAP-PATH LATENCY BUDGET: `Validate` sits in-line on the TLS-5 swap path, so
// its round trip is paid per swapped flow and is held to a swap-path latency
// budget (doc 16 §4 / §9). Grant fetch is per-grant and cached <= session
// (D39), never per-request, so steady-state swapped requests pay zero added RTT
// beyond this validation hop (doc 16 §5.1).
type IdentityValidationServiceClient interface {
	// Validate authorizes one swap-path credential presentation.
	//
	// FAILURE SURFACE: a DENY verdict surfaces to the agent as the in-band
	// structured 403 (D77 block+log default) — never a silent drop and never a
	// VM suspension for an ordinary policy question (doc 16 §4, §11.1 step 3).
	Validate(ctx context.Context, in *ValidateRequest, opts ...grpc.CallOption) (*ValidateResponse, error)
}

type identityValidationServiceClient struct {
	cc grpc.ClientConnInterface
}

func NewIdentityValidationServiceClient(cc grpc.ClientConnInterface) IdentityValidationServiceClient {
	return &identityValidationServiceClient{cc}
}

func (c *identityValidationServiceClient) Validate(ctx context.Context, in *ValidateRequest, opts ...grpc.CallOption) (*ValidateResponse, error) {
	cOpts := append([]grpc.CallOption{grpc.StaticMethod()}, opts...)
	out := new(ValidateResponse)
	err := c.cc.Invoke(ctx, IdentityValidationService_Validate_FullMethodName, in, out, cOpts...)
	if err != nil {
		return nil, err
	}
	return out, nil
}

// IdentityValidationServiceServer is the server API for IdentityValidationService service.
// All implementations must embed UnimplementedIdentityValidationServiceServer
// for forward compatibility.
//
// IdentityValidationService is the D22 sidecar seam, consumed by ds-tlsproxy on
// the TLS-5 swap path (doc 16 §4 / §9). The `IdentityValidation` service name in
// doc 16's prose sketch is rendered here with the proto house-style `Service`
// suffix (buf STANDARD); the seam identity is the D22 contract, unchanged.
//
// SYNC, session-liveness semantics: a single in-line call on the swap path.
// `Validate` checks signature + freshness + SESSION LIVENESS + grant lookup
// (doc 16 §4). The session-liveness check is what makes a stolen-but-unexpired
// credential fail immediately on suspend/kill/admin-revoke — the minimal CA
// ships no CRL/OCSP, so liveness, not revocation, is the catch (doc 16 §5.4).
//
// SWAP-PATH LATENCY BUDGET: `Validate` sits in-line on the TLS-5 swap path, so
// its round trip is paid per swapped flow and is held to a swap-path latency
// budget (doc 16 §4 / §9). Grant fetch is per-grant and cached <= session
// (D39), never per-request, so steady-state swapped requests pay zero added RTT
// beyond this validation hop (doc 16 §5.1).
type IdentityValidationServiceServer interface {
	// Validate authorizes one swap-path credential presentation.
	//
	// FAILURE SURFACE: a DENY verdict surfaces to the agent as the in-band
	// structured 403 (D77 block+log default) — never a silent drop and never a
	// VM suspension for an ordinary policy question (doc 16 §4, §11.1 step 3).
	Validate(context.Context, *ValidateRequest) (*ValidateResponse, error)
	mustEmbedUnimplementedIdentityValidationServiceServer()
}

// UnimplementedIdentityValidationServiceServer must be embedded to have
// forward compatible implementations.
//
// NOTE: this should be embedded by value instead of pointer to avoid a nil
// pointer dereference when methods are called.
type UnimplementedIdentityValidationServiceServer struct{}

func (UnimplementedIdentityValidationServiceServer) Validate(context.Context, *ValidateRequest) (*ValidateResponse, error) {
	return nil, status.Errorf(codes.Unimplemented, "method Validate not implemented")
}
func (UnimplementedIdentityValidationServiceServer) mustEmbedUnimplementedIdentityValidationServiceServer() {
}
func (UnimplementedIdentityValidationServiceServer) testEmbeddedByValue() {}

// UnsafeIdentityValidationServiceServer may be embedded to opt out of forward compatibility for this service.
// Use of this interface is not recommended, as added methods to IdentityValidationServiceServer will
// result in compilation errors.
type UnsafeIdentityValidationServiceServer interface {
	mustEmbedUnimplementedIdentityValidationServiceServer()
}

func RegisterIdentityValidationServiceServer(s grpc.ServiceRegistrar, srv IdentityValidationServiceServer) {
	// If the following call pancis, it indicates UnimplementedIdentityValidationServiceServer was
	// embedded by pointer and is nil.  This will cause panics if an
	// unimplemented method is ever invoked, so we test this at initialization
	// time to prevent it from happening at runtime later due to I/O.
	if t, ok := srv.(interface{ testEmbeddedByValue() }); ok {
		t.testEmbeddedByValue()
	}
	s.RegisterService(&IdentityValidationService_ServiceDesc, srv)
}

func _IdentityValidationService_Validate_Handler(srv interface{}, ctx context.Context, dec func(interface{}) error, interceptor grpc.UnaryServerInterceptor) (interface{}, error) {
	in := new(ValidateRequest)
	if err := dec(in); err != nil {
		return nil, err
	}
	if interceptor == nil {
		return srv.(IdentityValidationServiceServer).Validate(ctx, in)
	}
	info := &grpc.UnaryServerInfo{
		Server:     srv,
		FullMethod: IdentityValidationService_Validate_FullMethodName,
	}
	handler := func(ctx context.Context, req interface{}) (interface{}, error) {
		return srv.(IdentityValidationServiceServer).Validate(ctx, req.(*ValidateRequest))
	}
	return interceptor(ctx, in, info, handler)
}

// IdentityValidationService_ServiceDesc is the grpc.ServiceDesc for IdentityValidationService service.
// It's only intended for direct use with grpc.RegisterService,
// and not to be introspected or modified (even as a copy)
var IdentityValidationService_ServiceDesc = grpc.ServiceDesc{
	ServiceName: "dreamserpent.identity.v1.IdentityValidationService",
	HandlerType: (*IdentityValidationServiceServer)(nil),
	Methods: []grpc.MethodDesc{
		{
			MethodName: "Validate",
			Handler:    _IdentityValidationService_Validate_Handler,
		},
	},
	Streams:  []grpc.StreamDesc{},
	Metadata: "dreamserpent/identity/v1/validate.proto",
}
